version: "3.9"

services:
  http_requester:
    build: ./http_requester
    image: http_requester
    # network_mode: host
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      HOST: host.docker.internal